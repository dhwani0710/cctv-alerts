🔴 MUST FIX
- Fix `update_employee`'s photo validation: it calls `_validate_photo(photo)`, which doesn't exist (only `_validate_photo_upload` is defined) — this throws a `NameError` and crashes any employee update that includes a new photo.
- Fix README "Default Login" section: still says `admin / ceo123`; actual seeded default is `admin / admin123`.

🟠 SHOULD FIX
- Centralize API base URL into one shared config — currently each frontend file redeclares its own `API_BASE`/`API_URL` constant, and `AuditLogPage.jsx` still has a raw hardcoded `http://localhost:8000` with no env var at all.
- Add loading/error state handling to Records.jsx (list, delete, export, camera filter fetches all still silently fail).
- Delete the orphaned unreachable code block after the `return` in `is_within_store_hours()` in alerts.py (dead dedupe logic referencing undefined `person_name`/`priority`).
- Fix or remove `_get_known_faces_info()` in recognition.py — still has unconditional infinite self-recursion.
- Add `log_audit_event` calls to `add_zone`/`delete_zone` in main.py for consistency with other admin mutations.

🟡 OPTIONAL
- Add a duplicate-name guard on employee creation (two employees with the same name still silently share/clobber the same `known_faces/<name>` folder).
- Verify recognition.py's match-selection logic — confirm whether it still picks the first candidate folder clearing the threshold (dict-iteration order) rather than the closest match.
- Add RTSP URL format validation to AddCameraModal (still accepts any string).